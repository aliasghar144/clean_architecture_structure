name: clean_architecture
description: A template for clean architecture
version: 0.1.0+1

environment:
  mason: ">=0.1.0 <0.2.0"

vars:
  name:
    type: string
    description: The name of the feature
